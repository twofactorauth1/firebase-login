! function($) {
    "use strict";
    if ($.extend($.FroalaEditor.DEFAULTS, {}), $.FroalaEditor.PLUGINS.videoMediaManager = function(e) {

        }, !$.FroalaEditor.PLUGINS.video) throw new Error("Vedio manager plugin requires video plugin.");
    $.FroalaEditor.DEFAULTS.videoInsertButtons.push("videoMediaManager"), $.FroalaEditor.RegisterCommand("videoMediaManager", {
        title: "Media Manager",
        undo: !1,
        focus: !1,
        callback: function() {
        	this.video.showLayer("video-upload-media");
        },
        refresh: function(a) {
            this.video.refreshByMedia(a);
        }
    }), $.FroalaEditor.DefineIcon("videoMediaManager", {
        NAME: "video-camera"
    })
}(jQuery);
