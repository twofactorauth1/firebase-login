! function(e) {
    "use strict";
    if (e.extend(e.FroalaEditor.DEFAULTS, {}), e.FroalaEditor.PLUGINS.mediaManager = function(e) {
            
        }, !e.FroalaEditor.PLUGINS.image) throw new Error("Image manager plugin requires image plugin.");
    e.FroalaEditor.DEFAULTS.imageInsertButtons.push("mediaManager"), e.FroalaEditor.RegisterCommand("mediaManager", {
        title: "Media Manager",
        undo: !1,
        focus: !1,
        callback: function() {        	
        	this.$el.find(".fr-marker") && (this.events.disableBlur(), this.selection.restore());
            clickandInsertImageButton(this);
        }
    }), e.FroalaEditor.DefineIcon("mediaManager", {
        NAME: "fa fa-picture-o"
    })
}(jQuery);