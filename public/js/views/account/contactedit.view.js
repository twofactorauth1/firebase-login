define([
    'models/contact'
], function(Contact) {

    var view = Backbone.View.extend({

    });

    $$.v.account = $$.v.account || {};
    $$.v.account.ContactDetails = view;

    return view;
});

